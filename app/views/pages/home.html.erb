<main class="container">

  <h1>Qote Editor</h1>

  <p>A blazing fast quote editor built with Hotwire</p>

  <% if user_signed_in? %>
    <%= link_to "View Updates", quotes_path, class: "btn btn--dark" %>
  <% else %>
    <%= link_to "Sign In", new_user_session_path, class: "btn btn--dark" %>
  <% end %>

</main>
